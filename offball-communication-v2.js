import {MatchEngine} from './engine.js';
import {FIELD,onsideLimit} from './football-rules-v2.js';
import {chemistryAdjustedPassOptions,armChemistryPass} from './chemistry-decision-v1.js';

const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
function attackDir(team){return team===0?1:-1;}
function roleCanCall(p){return p&&p.role!=='GK'&&p.role!=='CB';}
function keepOnside(engine,p,x){if(!['ST','LW','RW','CAM'].includes(p.role))return x;const limit=onsideLimit(engine,p.team),dir=attackDir(p.team);return dir>0?Math.min(x,limit-5):Math.max(x,limit+5);}

export function callSignalFor(option,p){const kind=option?.kind||'support',dir=attackDir(p.team),lead=kind==='through'||kind==='lob-through'||kind==='progressive',ahead=dir*(Number(option?.aim?.x??p.x)-p.x)>22;if(lead&&ahead)return{label:'¡AL ESPACIO!',kind:'ahead'};if(kind==='cutback'||kind==='cross')return{label:'¡ACÁ!',kind:'box'};return{label:'¡DÁMELA!',kind:'feet'};}
export function bestCallForPass(engine,p,actor){if(!roleCanCall(p)||!actor||actor.team!==p.team||actor.id===p.id)return null;const option=chemistryAdjustedPassOptions(engine,actor).find(o=>o.player.id===p.id);if(!option||option.adjustedScore<.26||option.open<12)return null;const dir=attackDir(p.team),forward=dir*((option.aim?.x??p.x)-actor.x),spaceAhead=dir*((option.aim?.x??p.x)-p.x),progressive=['progressive','through','lob-through','cross','cutback'].includes(option.kind),score=option.adjustedScore+(progressive ? .10 : 0)+clamp(forward/260,-.15,.18)+clamp(spaceAhead/130,-.08,.14);if(score<.38)return null;const signal=callSignalFor(option,p);return{option,score,signal,target:{x:keepOnside(engine,p,option.aim.x),y:clamp(option.aim.y,FIELD.top+p.r,FIELD.bottom-p.r)}};}
function updateCalls(engine){const possession=engine.inferPossessionTeam?.();if(possession!==0&&possession!==1)return;const actor=engine.ballActor(possession);if(!actor)return;const candidates=[];for(const p of engine.players){if(p.team!==possession||p.id===actor.id||!roleCanCall(p))continue;const call=bestCallForPass(engine,p,actor);if(call)candidates.push({p,...call});}candidates.sort((a,b)=>b.score-a.score);const allowed=new Set(candidates.slice(0,3).map(c=>c.p.id));for(const p of engine.players){if(p.team!==possession||!roleCanCall(p))continue;if(!allowed.has(p.id)){if(p.callForPass&&p.callForPass.untilTick<engine.tick+4)p.callForPass=null;continue;}const c=candidates.find(x=>x.p.id===p.id),existing=p.callForPass;if(existing&&existing.untilTick>=engine.tick&&existing.fromId===actor.id)continue;const deep=c.signal.kind==='ahead',pace=Number(p.data?.pace??70),stamina=Number(p.data?.stamina??p.data?.physical??70);p.callForPass={fromId:actor.id,targetX:c.target.x,targetY:c.target.y,kind:c.signal.kind,label:c.signal.label,score:c.score,optionKind:c.option.kind,createdTick:engine.tick,untilTick:engine.tick+(deep?30:22)};p.action=c.signal.label;p.actionTimer=.48;if(deep&&pace>=58&&stamina>=52){p.burstTimer=Math.max(p.burstTimer||0,.24);p.burstX=attackDir(p.team);p.burstY=Math.sign(c.target.y-p.y)||0;}}}
const previousAiTarget=MatchEngine.prototype.aiTarget;
MatchEngine.prototype.aiTarget=function communicatedOffBallTarget(p,pressers,actor,possession){const base=previousAiTarget.call(this,p,pressers,actor,possession),call=p?.callForPass;if(!call||call.untilTick<this.tick||possession!==p.team||p.kickIntent||p.dribbleIntent||actor?.id===p.id)return base;const x=keepOnside(this,p,call.targetX);let weight=.58;if(call.kind==='ahead')weight=.84;else if(call.kind==='box')weight=.70;return{x:clamp(base.x*(1-weight)+x*weight,FIELD.left+p.r,FIELD.right-p.r),y:clamp(base.y*(1-weight)+call.targetY*weight,FIELD.top+p.r,FIELD.bottom-p.r)};};
const previousPrepare=MatchEngine.prototype.prepareBallAction;
MatchEngine.prototype.prepareBallAction=function respondToTeammateCall(p){const result=previousPrepare.call(this,p);if(!p||p.role==='GK')return result;const options=chemistryAdjustedPassOptions(this,p),calls=this.players.filter(m=>m.team===p.team&&m.callForPass?.fromId===p.id&&m.callForPass.untilTick>=this.tick).map(m=>({m,option:options.find(o=>o.player.id===m.id)})).filter(x=>x.option&&x.option.open>14).sort((a,b)=>(b.option.adjustedScore+(b.m.callForPass?.score||0)*.22)-(a.option.adjustedScore+(a.m.callForPass?.score||0)*.22)),best=calls[0];if(!best)return result;const requested=best.option.adjustedScore+(best.m.callForPass?.score||0)*.18,currentReceiver=p.kickIntent?.type==='pass'?p.kickIntent.receiverId:null,current=currentReceiver?options.find(o=>o.player.id===currentReceiver):null;if(!p.kickIntent&&p.dribbleIntent&&requested>.58){p.dribbleIntent=null;return armChemistryPass(this,p,best.option);}if(p.kickIntent?.type==='pass'&&currentReceiver!==best.m.id&&requested>(current?.adjustedScore??-.5)+.075){const old=this.playerById(currentReceiver);if(old?.receiveIntent?.fromId===p.id)old.receiveIntent=null;p.kickIntent=null;p.passIntent=null;return armChemistryPass(this,p,best.option);}return result;};
const previousStep=MatchEngine.prototype.step;
MatchEngine.prototype.step=function offBallCommunicationStep(dt){const out=previousStep.call(this,dt);if(!this.finished&&!this.restart?.active&&this.tick%14===0)updateCalls(this);for(const p of this.players)if(p.callForPass&&p.callForPass.untilTick<this.tick)p.callForPass=null;return out;};
export const __offballCommunicationV2={bestCallForPass,callSignalFor,updateCalls};

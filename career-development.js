const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
function hashSeed(seed){let h=2166136261;for(const ch of String(seed)){h^=ch.charCodeAt(0);h=Math.imul(h,16777619);}return h>>>0;}
function seeded(seed){let h=hashSeed(seed);return()=>{h+=0x6D2B79F5;let t=h;t=Math.imul(t^t>>>15,t|1);t^=t+Math.imul(t^t>>>7,t|61);return((t^t>>>14)>>>0)/4294967296;};}
export const DEVELOPMENT_STAGES=[{id:'academy',minAge:15,maxAge:17},{id:'rookie',minAge:18,maxAge:20},{id:'development',minAge:21,maxAge:23},{id:'prime',minAge:24,maxAge:29},{id:'veteran',minAge:30,maxAge:34},{id:'decline',minAge:35,maxAge:99}];
export function developmentStage(age){const n=clamp(Math.round(Number(age)||18),15,50);return DEVELOPMENT_STAGES.find(s=>n>=s.minAge&&n<=s.maxAge)?.id||'rookie';}
const START_LEVEL={academy:{base:51,spread:6},reserve:{base:55,spread:6},third:{base:57,spread:7},second:{base:60,spread:7},first_small:{base:62,spread:7}};
const BACKGROUND={local_academy:{start:0,potential:2},elite_academy:{start:3,potential:6},street:{start:-1,potential:4},late_bloomer:{start:-3,potential:7}};
export function createDevelopmentProfile({seed='career',age=17,entryLevel='reserve',background='local_academy'}={}){const r=seeded(`${seed}|${age}|${entryLevel}|${background}`),level=START_LEVEL[entryLevel]||START_LEVEL.reserve,bg=BACKGROUND[background]||BACKGROUND.local_academy;const startOverall=clamp(Math.round(level.base+bg.start+(r()-.5)*level.spread),45,68),potentialGap=12+Math.round(r()*15)+bg.potential,potential=clamp(startOverall+potentialGap,62,94),year=2026-clamp(Math.round(age),15,22),month=1+Math.floor(r()*12),day=1+Math.floor(r()*28);return{age:clamp(Math.round(age),15,22),birthDate:`${year}-${String(month).padStart(2,'0')}-${String(day).padStart(2,'0')}`,stage:developmentStage(age),entryLevel,background,startingOverall:startOverall,potential,dynamicPotential:potential,development:{technical:0,tactical:0,physical:0,experience:0},peakWindow:{from:24+Math.floor(r()*2),to:28+Math.floor(r()*3)},retirement:{eligibleFrom:33+Math.floor(r()*3),retired:false,reason:null}};}
const POSITION_WEIGHTS={
  GK:{defense:.30,composure:.18,physical:.14,ballControl:.12,vision:.10,passing:.08,pace:.04,stamina:.04},
  ST:{shooting:.29,pace:.16,dribbling:.16,ballControl:.14,composure:.13,physical:.08,passing:.04},LW:{pace:.23,dribbling:.22,ballControl:.15,shooting:.14,passing:.10,vision:.07,stamina:.05,composure:.04},RW:{pace:.23,dribbling:.22,ballControl:.15,shooting:.14,passing:.10,vision:.07,stamina:.05,composure:.04},CAM:{passing:.20,vision:.18,dribbling:.17,ballControl:.17,shooting:.11,composure:.08,pace:.05,stamina:.04},CM:{passing:.18,vision:.14,ballControl:.13,stamina:.13,dribbling:.10,defense:.10,physical:.09,composure:.07,pace:.04,shooting:.02},CDM:{defense:.22,physical:.17,stamina:.15,passing:.13,ballControl:.09,vision:.08,composure:.07,pace:.05,dribbling:.04},LB:{pace:.18,stamina:.17,defense:.17,physical:.13,passing:.12,dribbling:.09,ballControl:.07,vision:.04,composure:.03},RB:{pace:.18,stamina:.17,defense:.17,physical:.13,passing:.12,dribbling:.09,ballControl:.07,vision:.04,composure:.03},CB:{defense:.29,physical:.24,composure:.12,pace:.10,stamina:.08,passing:.07,ballControl:.05,vision:.03,dribbling:.02}
};
const DEVELOPMENT_ATTRIBUTES=['pace','shooting','passing','dribbling','defense','physical','ballControl','vision','stamina','composure'];
function weightedOverall(attributes,position){const weights=POSITION_WEIGHTS[position]||POSITION_WEIGHTS.CM;let sum=0,total=0;for(const [key,weight] of Object.entries(weights)){sum+=(Number(attributes[key])||50)*weight;total+=weight;}return total?sum/total:50;}
export function scaleAttributesToDevelopmentLevel(attributes,targetOverall,position='CM'){const target=clamp(Math.round(Number(targetOverall)||55),45,68),result={};for(const key of DEVELOPMENT_ATTRIBUTES)result[key]=clamp(Math.round(Number(attributes?.[key])||50),30,88);for(let pass=0;pass<5;pass++){const error=target-weightedOverall(result,position);if(Math.abs(error)<.35)break;for(const key of DEVELOPMENT_ATTRIBUTES)result[key]=clamp(Math.round(result[key]+error),30,88);}return result;}
const EARLY=new Set(['pace','physical','stamina']);const LATE=new Set(['vision','composure','tacticalIQ','anticipation','positioning']);
export function ageDevelopmentWeight(attribute,age){const a=clamp(Number(age)||18,15,45);if(EARLY.has(attribute)){if(a<=20)return 1.2;if(a<=25)return 1;if(a<=29)return .55;if(a<=32)return .1;return-.45-Math.min(.35,(a-33)*.04);}if(LATE.has(attribute)){if(a<=20)return .72;if(a<=24)return 1;if(a<=29)return 1.08;if(a<=33)return .55;return .08-Math.min(.28,(a-34)*.035);}if(a<=20)return 1;if(a<=25)return 1.05;if(a<=30)return .75;if(a<=33)return .3;return-.22-Math.min(.28,(a-34)*.03);}
export function developmentHeadroom(profile,currentOverall){const ceiling=Number(profile?.dynamicPotential??profile?.potential??currentOverall);return Math.max(0,ceiling-Number(currentOverall||0));}

const DEVELOPMENT_BUCKET={pace:'physical',physical:'physical',stamina:'physical',shooting:'technical',passing:'technical',dribbling:'technical',ballControl:'technical',defense:'tactical',vision:'tactical',composure:'tactical'};
function naturalGrowthPoints(headroom,level){if(headroom<=0)return 0;if(headroom>10)return 2;if(headroom>4)return level%3===0?2:1;return level%2===0?1:0;}
function naturalGrowthScore(player,attribute,level,buildMods){const weights=POSITION_WEIGHTS[player.position]||POSITION_WEIGHTS.CM,ageWeight=ageDevelopmentWeight(attribute,player.age??player.developmentProfile?.age??18);if(ageWeight<=0||Number(player[attribute])>=99)return-Infinity;const positional=Math.sqrt(Math.max(.01,weights[attribute]||.01))*40,build=Math.max(0,Number(buildMods?.[attribute])||0)*.65,headroomBias=Math.max(0,82-(Number(player[attribute])||50))*.025,r=seeded(`${player.instanceId||player.id||player.name}|natural-development|${level}|${attribute}`)();return positional+build+ageWeight*4+headroomBias+r*5;}
export function applyNaturalLevelDevelopment(player,{level=2,currentOverall,buildMods={}}={}){const profile=player?.developmentProfile;if(!player||!profile)return[];const calculatedOverall=Number.isFinite(Number(currentOverall))?Number(currentOverall):weightedOverall(player,player.position),displayedOverall=Number.isFinite(Number(player.rating))?Number(player.rating):calculatedOverall,overall=Math.max(calculatedOverall,displayedOverall),ceiling=Number(player.dynamicPotential??profile.dynamicPotential??profile.potential??overall),headroom=Math.max(0,ceiling-overall),points=naturalGrowthPoints(headroom,Math.max(2,Math.round(Number(level)||2)));profile.development??={technical:0,tactical:0,physical:0,experience:0};profile.development.experience=(Number(profile.development.experience)||0)+1;profile.age=Number(player.age)||profile.age;profile.stage=developmentStage(profile.age);if(points<=0)return[];const ranked=DEVELOPMENT_ATTRIBUTES.map(attribute=>({attribute,score:naturalGrowthScore(player,attribute,level,buildMods)})).filter(x=>Number.isFinite(x.score)).sort((a,b)=>b.score-a.score||a.attribute.localeCompare(b.attribute)),gains=[];for(const pick of ranked.slice(0,points)){const from=Number(player[pick.attribute])||50,to=clamp(from+1,30,99);if(to===from)continue;player[pick.attribute]=to;const bucket=DEVELOPMENT_BUCKET[pick.attribute]||'technical';profile.development[bucket]=(Number(profile.development[bucket])||0)+1;gains.push({attribute:pick.attribute,from,to,bucket});}return gains;}
